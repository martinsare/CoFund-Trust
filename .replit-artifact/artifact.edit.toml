kind = "mobile"
previewPath = "/"
title = "CoFund"
version = "1.0.0"
id = "cofund"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 20034

[services.development]
run = "npm run dev"

[services.production]
build = [ "npm", "run", "build" ]
run = [ "npm", "run", "serve" ]

[services.env]
PORT = "20034"
BASE_PATH = "/"
